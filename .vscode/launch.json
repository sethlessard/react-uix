{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 3000,
      "request": "attach",
      "type": "pwa-chrome",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Watch"
    }
  ]
}